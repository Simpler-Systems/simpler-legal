# lib-core — the vendored engine core (legal's own copy)

Copied from simpler-red's lib/ on 2026-07-24 per owner ruling: no dependency packages —
each vertical owns its engine ("treat legal as its own thing"). This is LEGAL'S copy; it
evolves under legal's bench and rulings. Improvements flow between verticals by deliberate
cherry-pick with measured verdicts, never by shared-package upgrade. Provenance: red at its
v1 release state (engine of record for red's shipped numbers).
